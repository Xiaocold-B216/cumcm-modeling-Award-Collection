# CUMCM Award Paper Corpus Analysis

This branch contains derived, traceable analysis assets for the excellent-paper corpus.

## Recovery mode

The previous temporary workspace is unavailable. All outputs on this branch are rebuilt from the trusted source baseline commit `a042ecf898feaba6fc81d543a10e0188db8b2b12` and must not be represented as recovered local artifacts.

## Repository boundaries

- Work only in `Xiaocold-B216/cumcm-modeling-Award-Collection`.
- Do not modify the materials repository.
- Do not modify the modeling skill-system repository.
- Keep original tracked source files read-only.
- Store derived outputs under `analysis-index/`, scripts under `scripts/`, and tests under `tests/`.

## Persistence rule

Each heavy phase must end with tests, a remote commit or deliverable recovery package, and a remote read-back verification before the next phase begins.
