
    # 基于局部最优模型的无人机位置调整策略与仿真定位：验证与稳健性

    ## 已观察到的验证证据

    - 用雷达多边形比较调整前后的编队误差。
- 绘制误差随循环次数下降曲线，并对多个初始扰动重复仿真。
- 给出三维点云与拟合平面，检查锥形编队几何一致性。
- 对正态分布微扰样本进行全局误差分析。

    ## 数据质量限制

    - 官方 B 题题面未提供。
- 附录程序为页面图像，未执行。
- 雷达图属于论文原有表达；知识库将其保留为证据，但不将其推荐为通用首选图形。

    ## 结论边界

    - 可以确认论文使用了上述验证结构和图表。
    - 不能据此声称代码已复现、最优性已被外部证明或摘要数值已独立验证。
    - `unknown`/`not_observed` 字段不进入“缺失特征为 absent”的分母。
