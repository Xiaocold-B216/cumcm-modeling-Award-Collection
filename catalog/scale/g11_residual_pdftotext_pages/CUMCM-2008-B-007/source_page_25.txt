11.附录
1.灰色预测过程：
1）数据准备
设参考数据为x 0 = x 0 1 , 𝑥 0 2 , … x 0 n ，计算数列的级比
x 0 (k − 1)
𝑎 k =
x 0 (k)

(k = 2,3, … , n)

交

流

如果𝑎 k 的范围超出了（0.5，1.5），可以适当考虑改变原序列元素为：
y (0) k = x 0 k + c
使得𝑎 k 在容许范围内。
2）模型建立
GM（1，1）表示模型为一元一阶方程的灰色模型，根据灰色理论中累加生成

研

指数模型的理论基础，对x 0 k 进行累加，生成序列：
x 1 = (x 1 1 , x 1 2 , … , x 1 n )

数学中国
www.madio.net
其中x 1 k = ∑ki=1 x 0 i

：
科

(k = 1,2, … , n).

令z (1) 为数列x (1) 的紧邻均值数列，令

(k = 2,3, … , n)

公

众

号

z 1 k = 0.5x 1 k − 1 + 0.5x 1 k ,
GM（1，1）模型灰微分方程为:
x 0 k + az 1 (k) = b
3）模型求解
将k = 2,3, … , n代入表达式，可得
x 0 2 + az 1 2 = b
x 0 3 + az 1 3 = b
……
x 0 n + az 1 n = b

微

信

−z 1 2 1
1
1
令Y = (x 0 2 , x 0 3 , … , x 0 n )T ,u = (a, b)T B = −z 3
…
…
−z 1 n 1
其中Y为数据向量，B为数据矩阵，u为方程参数向量，则GM（1，1）可以表
示为矩阵方程Y = B ∗ u
根据最小二乘原理有
u = (BT B)−1 BT Y
x 1 (t)满足常微分方程：

dx (1)
dt

+ ax (1) = b,可得x 1 t 的递推关系如下：

b
b
∗ e−at +
a
a
t+1 −x 1 t

x 1 t+1 = x 1 1 −

(t = 0,1, … , n − 1)

x 0 t+1 =x 1

(t = 0,1, … , n − 1)

25

