2、3 模型求解
2、3、1 具体算法如下：(应用改进的 dijkstra 算法进行求解)
Step1 : 对所有的节点 u ，
IF

u ≠ A, THEN

L(u ) = ∞; ELSE

L(u ) = 0 ； S = φ ；

Step 2 : 从不属于集合 S 的点中选取 L(u ) 取最小时的一个 u 值， S = S ∪ {u}；
Step3 : 对所有的节点 v ， v ∉ S ，
令 L(v) = Min[ L(v), L(u ) + w(u, v)] ， Pre(v) = u ，

IF

ELSE IF

R(u, v) ∩ Re (u ) = φ , THEN

Re (v) = R(u , v) ∩ Re (u ) ,η = 0 ；

Re (v) = R(u, v),η = 1 ；

GOTO

Step5; ELSE GOTO

研

ww

B ∈ S , THEN

交

t
ne
国 o.
中 di
学 ma
数 w.
ELSE

Step 4 : IF

Re (v) = R(u , v),η = 0 ；

u = A, THEN

流

其中， w(u, v) = t 0 (u , v) + η ⋅ λ ⋅ t w ， Pre(v) 为 v 点先驱，

Step 2 ；

：
科

Step5 : 形成最优路径 A(v 0 ) − v1 − v 2 − " − vi − vi +1 − " − v n − B(v n +1 ) ，通过 Pr e(v) 与 Re (v)

号

从终点 B 反向递归查找全路径与节点，算法结束。

2、3、2 对于公交网络实际问题的求解

公

众

我们应用上述算法对本问题进行求解，由于本题中的网络过于复杂，用改进的
dijkstra 算法仍有很高的时间复杂度和空间复杂度。本问题的公交网络中给出的站点数为
3000 多个，则要建立一个 3000*3000 邻接矩阵，我们用 matlab 编写程序对问题进行了
求解。

微

信

用模型二求的的结果与模型一相同，故结果在此不再赘述。

结果分析

问题二与问题一的结果相比，只有（5）、（6）不同。分析原因如下：
1、（5）（6）位于地铁沿线，当增加地铁这项交通工具时，首先会影响地铁沿线的站点
的路线选择。
2、增加地铁者这种交通方式后，
（6）找到了换乘次数更少的解，是因为地铁站点把两
个不相交的公交站点联结起来；
3、增加地铁者这种交通方式后，
（5）虽然没有找到换乘次数更少的解，但是找到了时
间更少的地铁线路，

17

