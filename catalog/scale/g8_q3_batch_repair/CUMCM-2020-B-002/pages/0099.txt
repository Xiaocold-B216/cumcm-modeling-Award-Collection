加 和 多 EEEAER if (vater_num + current_uater) * water_weight + (food_num + current_food) * food_weight > weight_limit: Print(' 负 重 过 大 '); game_over(); current_money -= spend_money; current_vater += water_num; current_food += food_num; def consume (multiple=1): global start_point, end_point, deadline, initial money, base_income, weight_ food_weight, food_price; 全 sy global water_weight, water_price, current_money, current_water, curren < current_position; /一 global money_earn_sum, water_consumption_sum, food_consumption_sum global weather_list weather=weather_list [current_date] consume_water = water_consumption_list[weather] * multiple; consume_food = food_consumption_list[ueather] * multiple; 证 consume_water > current_water or consume_food ood: print0' 食 物 /水 消耗 殉 尽 '); gatie_over(); return 0 # 0 表示 游戏 结束 ° current_water -= consume_water; 全 SEO 办 water_consumption_sum += _water; food_consumption_sum += ca ; return 1 一 = def compute_cons NT daynum, multiple_list): if dayni Oo pr 人 与 倍数 列表 不 匹配 '); exfsum = # 省 ; Q range (daynum) : ater_sum += multiple_list[i] * water_consumption_list[get_weather(begin_date + i)1; food_sum += multiple_list[i] * food_consumption_list[get_weather (begin_date + i)]; return water_sum, food_sum; def sell(food,uater): global water_price,food_price return food+0.5*food_price+waters0.5+uater_price def main_game(): 99