回 成 这 人 p AI <) EEEE from sklearn.model_selection import train_test_split X_train, X_test, y_train, y_test = train_test split(X_trainl, y, random_state=1) #x_train.to_excel('xx_trans.xls') #y_train.to_excel('y_trans.xls') #call decision tree #from sklearn import tree #clf = tree.DecisionTreeClassifier(max_depth=10) #clf = cIf.fit(X_train, y_train) from keras.models import Sequential # from keras.layers.core import Dense, Dropout, Activation 全 AY o model = Sequential() # 建 立 模型 7 \) model.add(Dense(units=48,input_dim = 12)) #7RIIHIAE Feij 3 modelLadd(Activation(tanh?)# 以 Relu 函数 为 激活 函数 model.add(Dense(input_dim = 48, units=48)) # 添 加 人 层 的 连接 modelLadd(Activation(relu7)# 以 Relu 函数 为 激活 瑟 modeladd(Dropout(0.2)) X 一 / model.add(Dense(input_dim = 48,units= 36)) 基 基 加 陷 葡 层 、 隐 藏 层 的 连接 model.add(Activation('relu')) # 以 Relu 函数 为 激活 函数 modeladd(Dropout(0.2)) 个 model.add(Densc(input dim 一 i8=36))#Vs MBRUZ « BEGHUZ [17E £ model.add(Activation('relu’)) Re 人 数 为 激活 函数 Vd model.add(Dense(input. dim= 36, units=12)) # 添 加 隐藏 层 、 隐 藏 层 的 连接 aaabnt L Relu 函数 为 激活 函数 model.add(Dense(input ¢ 12, units=12)) # 添 加 隐藏 层 、 隐 藏 层 的 连接 model.add(Activation('rel ) # 以 Relu 函数 为 激活 函数 modela on 六 na 输出 层 的 连接 maaeladd(Activation(sigmoid))# 以 sigmoid 函数 为 激活 函数 # 纲 缉 横 明 / 损失 函数 为 binary_crossentropy， 用 adam 法 求解 NmnodtLcampiledloss='mean squared_error, optimizer="adam’) W model fit(x_train.values, y_train.values, batch_size = 2000) #ll 4Hi%4 Ts pd.DataFrame(modelpredict_ classes(x_test.values)) IT=pd.DataFrame(modelpredict(x_testvalues)) rr=rvalues tr=rrflatten() foriin range(trshape[0]): 让 tr[i]>0.5: tr[i]=1 else: 63/77