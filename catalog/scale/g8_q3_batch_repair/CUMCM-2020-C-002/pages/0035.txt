回 矶 这 是 了 四 j=j+1; end minusPortion(i) = sumMinus/numEachFirm12(i); end clear ii clear j; badPortion = 0.3*cancelPortion + 0.7*minusPortion; %% 主 成 分 分 析 ， 信 贷 风险 因子 计算 alllndex = [sumProfit progressIndex preference tradPattern... creditRank violation badPortion]; alllndex = zscore(alllndex); 四 relateMatrix = corrcoef(alllndex); 四 从 & [featureVec,featureVal,contriRate] = pcacov(relateMatrix); o 让 fori= 1:7 7 forj=1:7 ) if featureVec(i,j) < 0 featureVec(i,j) = -1*featureVec(ij); end 4s end o end 二 num = 6; df = alllndex*featureVec(1:num,:)’; tf= df*contriRate(1:num)/100; o [~ind] = sort(tf,'descend’); &, o tf= (tf - min(tf)) ./ (max(tf) - min(tf)); 人 =< save('riskIndex_formI.mat','fi % redit]''tf,'ind','sumProfit',... ‘progressIndex','preference’, tradPattern','creditRank', 'violation',... ‘badPortion’); = r/ | 2. all_G 5 % 该 程序 运 重 革 闻 约 为 20 秒 AL se all; clc; load formlcredit'; load riskImndex_forml; load pofViolation_forml; clear badPortion; clear preference; clear progressindex; clear sumProfit; clear tf; clear tradPattern; clear ind; 35