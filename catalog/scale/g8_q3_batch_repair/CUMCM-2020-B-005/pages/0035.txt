回 矶 这 全 关 2 } if(day>=date) { act[day]=-1; rec[day]=-1; return; } 这 qua[now] 一 1) nmEMaxm-mx*x-my*y; for(int i=0;i<n;i++) if(flquafnow]][quali]}) 全 % int tx=costx[day][now][i]; e SS int ty=costy[day][now][i]; 二 \ int ucost=c; ) int ux,uy; } int um=nm; if(x>=tx) 4s UxX=x-tx; 全 else 二 ux=0; ucost-=2%(tx-x)*cx; e um-=(tx-x)*mx; 全 %% if(y>=ty) uy=y-ty; 3 p se oj 2 -2*#(ty-y)#cy; JU1 ucdst (yy)*ey; 和 个 W if(ucost<0]jum<0) N continue; dfs(day+days[day][now][i].i,um,ucost,ux,uy,0); } if(qua[now]==2) { int attday=day; int tx=sx[weal[attday]]; int ty=sy[wea[attday]]; attday++; 35