ElRi: 和 放生 入 return; } if(qua[now]=1) nmEMaxm-mx*x-my*y; for(int i=0;i<n;i++) if(tTquanow][quafill) { int tx=costx[day][now][i]; int ty=costy[day][now][i]; int ucost=c; int ux,uy; “ int um=nm; 全 #z if(x>=tx) 全 SS UX=X-tX; 2 else H- ux=0; ucost-=2*(tx-x)*cx; 4s um-=(tx-x)*¥mx; 全 - if(y>=ty) uy=y-ty; 个 else ° 1 % uy=0; ucost-=2% cy; um-=(ty-y) ¥ } if(ucost<0/um<0) continue; hin days[day][now][i].i,um,ucost,ux,uy,0); } 了 Ke: \E AW TAR t attday=day; int tx=sx[wea[attday]]; int ty=sy[wea[attday]]; attday++; if(x>=tx) { X-=tX; tx=0; } 30