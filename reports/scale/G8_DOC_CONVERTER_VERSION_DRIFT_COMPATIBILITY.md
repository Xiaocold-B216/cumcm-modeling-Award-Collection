# G8 DOC Converter Version Drift Compatibility

Status: `PARTIAL`

Candidate build: `16.0.20326.20112`; frozen baseline: `16.0.20228.20190`.
Word sessions: `2`; conversion attempts: `2`; semantic passes: `0/3`.

Candidate PDFs are isolated under `tmp/g8_doc_converter_version_drift/16.0.20326.20112/`; governed production outputs were not written.

Binary PDF SHA equality is not required; semantic equivalence is evaluated from page count, page class, OCR route, page text hashes, and normalized text hash.

This runner must be executed only by the user in a normal Windows desktop Word session.
